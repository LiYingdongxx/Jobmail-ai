# JobMail AI 项目说明

## 1. 项目名称

`JobMail AI：求职邮件智能助手`

## 2. 项目背景

在实习和校招求职过程中，候选人往往会同时投递多个岗位，收到的邮件类型也非常复杂，包括面试邀请、笔试通知、材料补交、进度反馈、Offer 沟通以及大量营销信息。  
这些邮件中的关键信息通常分散在主题和正文里，人工逐封整理不仅耗时，而且很容易漏掉截止时间、待办事项和重要回复节点。

基于这个问题，我将一个通用邮件处理 Demo 改造成了一个面向求职场景的 AI 助手，希望帮助用户更高效地处理招聘相关邮件，降低遗漏风险，提高求职沟通效率。

## 3. 目标用户

本项目的目标用户是：

- 正在寻找实习机会的本科生和研究生
- 参与校招流程的应届毕业生
- 同时投递多个岗位、需要统一管理招聘邮件的求职者

## 4. 核心问题

这个项目主要解决以下几个问题：

- 招聘邮件类型多，人工分类和整理成本高
- 面试时间、截止时间、补交材料要求容易遗漏
- 正式邮件回复需要措辞得体，准备成本较高
- 在多个岗位并行推进时，用户很难快速判断每封邮件“下一步该做什么”

## 5. 项目目标

本项目希望实现一个最小可用的求职邮件助手，帮助用户完成以下任务：

1. 自动识别招聘邮件类型
2. 提取邮件中的关键时间、联系方式和待办事项
3. 根据不同场景生成回复草稿
4. 给出明确的下一步行动建议

## 6. MVP 功能

当前版本的最小可用功能包括：

### 6.1 邮件分类

将邮件划分为以下 6 类：

- `interview`
- `materials`
- `follow_up`
- `offer`
- `rejection`
- `spam`

### 6.2 关键信息提取

从邮件正文中提取：

- 日期
- 时间
- 手机号
- 邮箱
- 待办事项

### 6.3 回复草稿生成

针对以下场景生成模板化回复：

- 面试确认
- 材料补交
- Offer 沟通

### 6.4 行动建议

根据邮件类型输出下一步建议，例如：

- 立即确认面试
- 准备并补交作品集
- 继续等待，无需立刻回复
- 确认到岗时间
- 归档拒信
- 忽略营销邮件

## 7. 为什么适合用 AI

求职邮件助手是一个典型的文本理解与决策辅助场景，适合使用 AI 的原因包括：

- 邮件内容本质上是自然语言文本，适合进行语义分类
- 关键信息提取属于典型的信息抽取任务
- 邮件回复草稿生成属于自然语言生成任务
- 用户真正需要的不是“全文摘要”，而是“我下一步该怎么做”，这非常适合由 AI 提供辅助建议

## 8. 为什么不做全自动发送

虽然 AI 可以生成邮件回复，但本项目不将“自动发送”作为当前阶段的默认能力，原因是：

- 求职沟通属于正式、高风险沟通场景
- 回复内容涉及时间确认、材料承诺、联系方式等重要信息
- 一旦自动发送错误内容，可能直接影响求职结果

因此，本项目当前定位是：

`生成建议 + 保留人工确认`

## 9. 当前实现思路

当前版本采用“规则分类 + 正则提取 + 模板回复”的轻量方案，优先验证产品流程，而不是一开始就引入复杂模型。

### 9.1 分类逻辑

通过关键词规则对邮件进行初步分类，例如：

- 面试 / assessment -> `interview`
- 成绩单 / portfolio -> `materials`
- status / update -> `follow_up`
- offer / onboard -> `offer`
- regret / 未通过 -> `rejection`
- promotion / 优惠 -> `spam`

### 9.2 信息提取逻辑

通过正则表达式提取：

- 日期
- 时间
- 手机号
- 邮箱地址

并通过关键词句匹配提取待办事项。

### 9.3 回复生成逻辑

根据分类结果和邮件语言，调用不同回复模板，生成适合当前场景的确认型草稿。

## 10. 下一步优化方向

为了让这个项目更接近真实可用的 AI 产品，下一步计划围绕以下方向改进：

1. 将演示数据从代码中拆出，支持独立样本集和评测
2. 增加结构化字段提取：
   - `company`
   - `position`
   - `deadline`
   - `interview_mode`
   - `action_suggestion`
3. 为不同类型邮件增加更细的策略逻辑
4. 增加轻量前端页面，例如 Streamlit 展示
5. 建立分类准确率、提取准确率、回复可用率等评估指标

## 11. 项目价值

这个项目的价值不在于“是否用了最复杂的模型”，而在于：

- 选取了一个真实且高频的求职场景
- 把邮件分类、信息提取、回复生成和行动建议整合成了一个完整流程
- 体现了 AI 产品设计中非常重要的能力：
  - 问题定义
  - 场景拆解
  - MVP 设计
  - 风险边界控制
  - 结果可解释性

## 12. 一句话介绍版本

`JobMail AI 是一个面向实习和校招场景的求职邮件智能助手，能够自动分类招聘邮件、提取关键信息、生成回复草稿，并为用户提供下一步行动建议。`
